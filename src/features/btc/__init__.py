
import subprocess
subprocess.run(['pip', 'install', 'pandas_datareader'], check=True, text=True)
subprocess.run(['pip', 'install', 'google-oauth'], check=True, text=True)
subprocess.run(['pip', 'install', 'pandas-gbq'], check=True, text=True)
# subprocess.run(['pip', 'install', 'pandas_gbq'], check=True, text=True)
subprocess.run(['pip', 'install', 'pickle-mixin'], check=True, text=True)
subprocess.run(['pip', 'install', 'joblib'], check=True, text=True)
